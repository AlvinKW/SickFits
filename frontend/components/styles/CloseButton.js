import styled from 'styled-components';

const CloseButton = styled.button`
	position: absolute;
	right: 0;
	border: 0;
	font-size: 3rem;
	background: black;
	color: white;
	z-index: 2;
	cursor: pointer;
`;

export default CloseButton;
